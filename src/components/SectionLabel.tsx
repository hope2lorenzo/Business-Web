const SectionLabel = ({ children }: { children: React.ReactNode }) => (
  <span className="text-xs font-bold tracking-[0.2em] uppercase text-primary">{children}</span>
);

export default SectionLabel;
